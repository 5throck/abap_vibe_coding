# Task: Fix Gemini CLI Hook Error

> [!NOTE]
> This is a historical task log representing a completed session.
> For current active context templates and agent paths, please refer to the standard templates in `docs/task-template.md` and `agents/`.

## 0. Request
> Gemini CLI에 설정 문제가 발생했어.
> Invalid hook event name: "PostToolUse" from project config. Skipping.

## 1. Analysis
- **Symptom**: Gemini CLI reporting "Invalid hook event name: PostToolUse".
- **Cause**: PostToolUse is a Claude Code-specific hook event name. Gemini CLI uses AfterTool.

## 2. Implementation
- [x] **[MODIFY]** .gemini/settings.json: Renamed PostToolUse to AfterTool.
- [x] **[MODIFY]** GEMINI.md: Updated documentation to reflect AfterTool.
- [x] **[MODIFY]** docs/setup-guide.md: Updated documentation to reflect AfterTool.
- [x] **[MODIFY]** CLAUDE.md: Updated cross-reference to reflect AfterTool.

## 3. Verification
- [x] Ran ash scripts/sync-md.sh -> Audit PASSED.
- [x] Verified file contents.

---
*Status: Fixed*

## 4. Matcher Issue Analysis
- **Finding**: In `hyperfocused` mode, all tools are routed via `sap_execute`.
- **Impact**: The matcher `"Write|Edit"` will not trigger because it matches against the tool name, not the inner action.
- **Fix**: Update `.gemini/settings.json` matcher to `"sap_execute"`.
