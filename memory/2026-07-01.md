# Development Log — 2026-07-01

---

## docs: port mig improvements and fix encoding regressions

**Summary**: Compared the `abap_vibe_coding_mig` sibling project (separately git-initialized, 2 local commits of governance/audit fixes) against this project and reflected the safe, applicable improvements. Finalized already-partially-ported files, fixed encoding regressions left over from that partial port, and applied one targeted audit-script fix.

**Files Changed**:
- `agents/pm.md`, `CLAUDE.md`, `GEMINI.md` — stripped stray UTF-8 BOM and restored mangled em-dash/arrow/section-sign/emoji characters
- `scripts/README.md` — removed a literal `\n` regression that duplicated two table rows
- `scripts/setup.ps1` — fixed a pre-existing header-comment typo (also present in mig)
- `scripts/audit.ps1`, `scripts/audit.sh` — CONSTITUTION.md accessibility check now also looks at `../../CONSTITUTION.md` to match this project's `Projects/<name>/` nesting depth (mirrors mig commit `a255e6c`)
- `scripts/agent-create.ts`, `scripts/agent-delete.ts`, `scripts/agent-list.ts`, `scripts/agent-verify.ts`, `docs/README(.ko).md`, `skills/README(.ko).md`, `scripts/README_ko.md`, `agents/handoff-spec_ko.md`, `package.json`, `bun.lock` — verified byte-identical to mig source (already staged as untracked from a prior session), no changes needed
- `CHANGELOG.md` — added Added/Fixed entries for the above

**Decisions**: Deliberately excluded mig's full `audit.ts`/`dispatch.ts` TypeScript architecture migration (`.claude/audit-baseline.json`, `docs/governance/G0001-G0003`, `docs/adr/0021-0022`, `docs/validation/*`, ~20 rewritten `scripts/*.ts`). This project still runs on the `.ps1`/`.sh` hybrid script model; adopting the TS-first architecture is a separate, larger decision the user chose to defer to its own design review rather than bundle into this port.

**Issues**: None — `scripts/vsp-audit.ps1` passes all checks after the fixes above.

---

## docs: port mig improvements and fix encoding regressions
- **Files**: .claude/settings.json,CHANGELOG.md CLAUDE.md,GEMINI.md SECURITY.md,agents/handoff-spec.md agents/pm.md,docs/context.md docs/security.md,docs/tooling-matrix.md memory/2026-05-25.md,scratch/temp/check-encoding.ps1 scripts/README.md,scripts/audit.ps1 scripts/audit.sh,scripts/dev-sync.ps1 scripts/gen-pr-body.ps1,scripts/git-sync.ps1 scripts/install-bun.ps1,scripts/install-vsp.ps1 scripts/package.json,scripts/setup.ps1 scripts/sync-md.ps1,scripts/vsp-audit.ps1 scripts/vsp-publish.ps1,scripts/vsp-sync.ps1 scripts/vsp-task.ps1,agents/handoff-spec_ko.md bun.lock,docs/README.md docs/README_ko.md,memory/2026-07-01.md package.json,scripts/README_ko.md scripts/agent-create.ts,scripts/agent-delete.ts scripts/agent-list.ts,scripts/agent-verify.ts skills/README.md,skills/README_ko.md
- **Purpose**: 
- **Decisions**: 
- **Issues**: None
