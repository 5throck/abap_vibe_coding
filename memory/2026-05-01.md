# MEMORY.md

Records ABAP objects created or modified during development sessions, along with key decisions and issue history.

---

## ABAP Programs

### ZPROG_SBOOK_QUERY — Flight Booking Query Report

- **Created**: 2026-04-29
- **Package**: `$TMP`
- **Object URL**: `/sap/bc/adt/programs/programs/ZPROG_SBOOK_QUERY`

#### Purpose
Queries the Sflight demo database across four joined tables (SBOOK, SCARR, SPFLI, SCUSTOM) and displays results in a `CL_SALV_TABLE` ALV grid with built-in sort, filter, and Excel export.

#### Selection Screen Fields
| Variable | Label | Source Field |
|----------|-------|--------------|
| `s_carrid` | Airline Code | `SBOOK-CARRID` |
| `s_connid` | Connection No. | `SBOOK-CONNID` |
| `s_fldate` | Flight Date | `SBOOK-FLDATE` (default: today) |
| `s_custid` | Customer ID | `SBOOK-CUSTOMID` |
| `s_class` | Seat Class | `SBOOK-CLASS` (Y/C/F) |
| `p_cancel` | Include Cancelled | `SBOOK-CANCELLED` |

#### Key Technical Decisions
- **Selection screen field labels**: Set at runtime in `INITIALIZATION` via `%_<field>_%_app_%-text` variables.
- **ALV column headers**: `set_short/medium/long_text` not called — ALV automatically reads labels from DDIC data elements.
- **ALV title**: `set_list_header( 'Flight Booking Query Report (ALV)' )`
- **MANDT in JOIN**: Cannot be used in `ON` condition — handled automatically by the compiler; removed from all JOIN clauses.
- **CASE in Open SQL**: Not supported in ABAP 7.52 — replaced with two separate `SELECT` statements under an `IF p_cancel` branch.
- **SELECT-OPTIONS name limit**: Maximum 8 characters — shortened from `so_carrid` to `s_carrid`, etc.
- **SELECTION-SCREEN BLOCK TITLE**: Text symbols (`TEXT-xxx`) cannot be assigned at runtime — removed `TITLE` clause; using `WITH FRAME` only.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| 403 on object create | DEVELOPER user had no development license | Registered NPL license via SLICENSE → SAP Minisap portal |
| `TEXT-B01` cannot be modified | Text symbols are read-only at runtime | Removed `TITLE` clause; kept `WITH FRAME` only |
| `MANDT` in ON condition error | Client field is compiler-managed | Removed all `mandt` conditions from JOIN `ON` clauses |
| SELECT-OPTION name too long | 8-character name limit | Renamed `so_carrid` → `s_carrid`, etc. |
| BLOCK TITLE variable name error | 8-character limit also applies to TITLE reference | Switched to `WITH FRAME` only |
| `CASE` not supported in Open SQL | ABAP 7.52 restriction | Replaced with `IF p_cancel` branching two SELECT statements |

### ZPROG_EPM_DEMO — EPM Sales Order Dashboard

- **Created**: 2026-05-01
- **Package**: `$TMP`
- **Object URL**: `/sap/bc/adt/programs/programs/ZPROG_EPM_DEMO`

#### Purpose
Demonstrates utilization of the Enterprise Procurement Model (EPM) tables (`SNWD_SO`, `SNWD_BPA`) as an alternative to SFLIGHT. Features a selection screen with dynamic labels and ALV output via `CL_SALV_TABLE`.

#### Selection Screen Fields
| Variable | Label (Description) | Source Field |
|----------|---------------------|--------------|
| `s_soid` | Sales Order ID | `SNWD_SO-SO_ID` |
| `s_bpan` | Company Name | `SNWD_BPA-COMPANY_NAME` |

#### Key Technical Decisions
- **EPM Model Selection**: Chose EPM (`SNWD_` tables) as the modern 7.5x alternative to SFLIGHT.
- **Selection Screen Labels**: Used `SELECTION-SCREEN COMMENT` combined with `INITIALIZATION` to manually set labels, 정제된 Description을 사용하도록 조치함 (removed "EPM:" prefixes).
- **ALV Implementation**: Used `cl_salv_table=>factory` for modern grid output with standard functions enabled.
- **Non-Flat Structure Handling**: `SNWD_BPA` contains a `STRING` field (`WEB_ADDRESS`), making it a deep structure. Replaced `TABLES` declaration with local `DATA` variables for `SELECT-OPTIONS`.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| ADT Lock Error (423) | Known bug in `vsp deploy` for program updates | Used `vsp source write` with piped input as a workaround. |
| Selection Screen "???" | Encoding mismatch for Korean literals | Switched block title to English: 'EPM Sales Order Search'. |
| TABLES Syntax Error | `SNWD_BPA` is not a flat structure | Declared local variables for `SELECT-OPTIONS` instead of using `TABLES`. |
| Program Title "???" | Missing TEXTPOOL ID 'R' | Manually inserted TEXTPOOL entry for EN and KO via `INSERT TEXTPOOL` script. |

---

## MCP Configuration

### Actual connection source: `claude_desktop_config.json` (global)
- **Binary**: `C:\git\abap\vsp.exe`
- **Args**: `[]` (none)
- **Credentials**: injected via `env` block (`SAP_URL`, `SAP_USER`, `SAP_PASSWORD`, `SAP_CLIENT`, `SAP_MODE`)
- The global config takes precedence over the project `.mcp.json`.

### Project `.mcp.json` (`C:\git\abap\.mcp.json`)
- Secondary config; kept in sync with global for consistency.
- `command`: `C:\git\abap\vsp.exe`, `args`: `[]`

### `.env` (`C:\git\abap\.env`) — gitignored
- Fallback for CLI direct execution (`vsp system info`, etc.)
- Contains `SAP_URL`, `SAP_USER`, `SAP_CLIENT`, `SAP_LANGUAGE`; loaded automatically via `godotenv`.

### `.vsp.json` — deleted
- Was used with `-s npl` flag; no longer needed.
- All credentials now supplied via env vars in global config and `.env`.

### Change history
| Date | Change |
|------|--------|
| 2026-04-29 | Initial: `${SAP_URL}` env placeholders in `.mcp.json` — failed (not expanded by Claude Code) |
| 2026-04-29 | Switched to `-s npl` flag → reads from `.vsp.json` |
| 2026-04-29 | Migrated to `.env` + args `[]`; discovered global config is actual connection source |
| 2026-04-29 | Deleted `.vsp.json`; fixed `.mcp.json` command path to `C:\git\abap\vsp.exe` |
| 2026-05-01 | Deployed ZADT_VSP objects with 7.52 compatibility patches. |
| 2026-05-01 | Configured hyperfocused mode in `.gemini/settings.json`, `.mcp.json`, and `.env`. |
| 2026-05-01 | Created `SKILL.md` and installed `/celebrate` skill in `.claude/commands/`. |
| 2026-05-01 | Documented SAP Gateway & OData demo data and created troubleshooting guide for /IWFND/MAINT_SERVICE. |

---

## SAP Gateway & OData Reference

### Documentation Artifacts
- **[walkthrough.md](file:///C:/Users/USER/.gemini/antigravity/brain/9c6cecd3-bc4b-49be-aca1-061763365ab1/walkthrough.md)**: Detailed guide for `GWSAMPLE_BASIC` OData service and `SEPM/EPM` data model.
- **[troubleshooting_guide.md](file:///C:/Users/USER/.gemini/antigravity/brain/9c6cecd3-bc4b-49be-aca1-061763365ab1/troubleshooting_guide.md)**: Resolution steps for `/IWFND/MAINT_SERVICE` execution failures.

### Key Technical Decisions
- **EPM Table Prefix**: Confirmed usage of `SNWD_*` tables for OData demo data.
- **Data Generation**: Identified `SEPM_DG` as the primary tool for populating demo tables.
- **SICF Path Versioning**: Discovered that in newer SAP versions (e.g., S/4HANA), the Gateway maintenance path has moved from `/sap/opu/iwfnd/` to `/sap/opu/odata/iwfnd/`.

### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| `/IWFND/MAINT_SERVICE` execution failed | ICF node for the maintenance UI was inactive | Activated node via `SICF`. |
| Mismatch in SICF path | Path differs across SAP versions | Verified correct path for the specific system via browser research and user screenshot: `/sap/opu/odata/iwfnd/maint_service`. |

---

## ZPROG_SBOOK_QUERY — OO-ABAP Refactoring (2026-05-01 Session 2)

### Changes Applied
Full refactoring from procedural (`FORM`-based) to Object-Oriented ABAP:

1. **Class `LCL_REPORT`** with `FINAL` modifier introduced:
   - `RUN` — public entry point called from `START-OF-SELECTION`
   - `FETCH_DATA` — private, single optimised SELECT
   - `DISPLAY_ALV` — private, CL_SALV_TABLE display
   - `SET_COL_TEXT` — helper; sets long/medium/short column header via typed IMPORTING params (`scrtext_l/m/s`)

2. **SQL Consolidated**: Two duplicate `SELECT` statements unified with:
   ```abap
   AND ( @p_cancel = @abap_true OR bk~cancelled = @abap_false )
   ```

3. **UI/UX**: Progress indicator, striped ALV, Korean column headers for CARRNAME, CUSTNAME, CITYFROM, CITYTO, FLDATE.

### Issue History (this session)
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| UTF-8 BOM breaks deploy | `WriteAllText` default includes BOM | Use `New-Object System.Text.UTF8Encoding($false)` |
| Korean garbled via pipe | PowerShell pipe re-encodes to ANSI | Use `vsp source edit --old --new` for Korean string patches |
| Lock handle 423 on deploy | Stale lock from previous write session | Use `vsp source write` (auto lock/unlock) instead of `vsp deploy` for existing objects |
| `TEXT-xxx` references | Text symbols require SE38 manual entry | Replaced with Korean inline literals; INITIALIZATION `%_x_%_app_%-text` works correctly |

### Lint Results (post-refactor)
- 2 warnings (style): `colon_missing_space`, `local_variable_names`
- No errors — activated successfully

---
