# Memory Log: 2026-05-21

## Consistency Audit & Documentation Overhaul

- **Files**: AGENTS.md, GEMINI.md, docs/tooling-matrix.md, docs/setup-guide.md, docs/plugin-setup.md, docs/mcp_usage.md, docs/testing-guidelines.md, agents/sap-investigator.md, agents/architect.md
- **Purpose**: Full cross-file consistency audit and fix (IMP-01 through IMP-10 completion verification)
- **Decisions**:
  - Removed all `browser_subagent` references (tool does not exist in vsp MCP server)
  - Fixed all `VSP_*` → `SAP_*` prefix inconsistencies throughout docs
  - Fixed hyperfocused mode description: "all 101 ops via sap_execute routing" (not "1 tool")
  - Fixed CO trigger keyword `CSKP` → `CSKB` in AGENTS.md and agents/sap-investigator.md
  - Fixed Role Boundary Matrix: "Analyse ABAP source logic" → `architect` (not `sap-investigator`)
  - Removed non-existent `harness:memory-intelligence` skill reference from Intelligence Investigator

## AGENTS.md Dispatch-Card Refactoring

- **Files**: AGENTS.md
- **Purpose**: Convert all Business Group and Technical Group entries to dispatch-card format
- **Decisions**:
  - Business Group (SD/LE/PP/MM/FI/CO): removed `Allowed Tools` + `Output Format` skeleton blocks; renamed `Context file` → `Subagent prompt`
  - Technical Group (10 agents): replaced `Responsibilities` detail bullets with `When to dispatch` + `Output` + `Subagent prompt`
  - Added missing `Subagent prompt` links for all 10 technical agents
  - Added Technical Group intro: "Architect acts as Technical Execution Lead"
  - Architect card: added `Technical Lead responsibilities` field
  - PM entry: converted to dispatch-card — removed redundant `§5 Finalization` steps; added `Entry point` + `Subagent prompt` fields

## Architect as Technical Execution Lead

- **Files**: agents/architect.md, AGENTS.md
- **Purpose**: Formally designate Architect as single point of contact between PM and Technical Group
- **Decisions**: Opening statement updated; AGENTS.md Technical Group intro updated

## Strategic BAPIs Expansion (Option B — Full Lifecycle)

- **Files**: skills/sap-sd/SKILL.md, skills/sap-mm/SKILL.md, skills/sap-fi/SKILL.md, skills/sap-co/SKILL.md, skills/sap-pp/SKILL.md, skills/sap-le/SKILL.md
- **Purpose**: Expand all 6 module skills to Create/Change/Cancel/Confirm BAPI lifecycle coverage
- **Decisions**:
  - SD: Fixed `TARGET_QUY` → `TARGET_QTY` typo; expanded `BAPI_OUTB_DELIVERY_CREATE_SLS` stub to full params; added `BAPI_SALESORDER_CHANGE`, `BAPI_BILLINGDOC_CREATEMULTIPLE`
  - MM: Added `BAPI_PO_CHANGE`, `BAPI_MATERIAL_SAVEDATA`
  - FI: Added `BAPI_ACC_DOCUMENT_REV_POST`, `BAPI_INCOMINGINVOICE_CREATE`; expanded POST params
  - CO: Added `BAPI_COSTCENTER_CHANGE`, `BAPI_INTERNALORDER_CREATE`
  - PP: Added `BAPI_PRODORD_RELEASE`, `BAPI_PRODORD_COMPLETE_CONF`
  - LE: Added `BAPI_OUTB_DELIVERY_CONFIRM_DEC`, `BAPI_WHSE_TO_CONFIRM`

## Project Constitution Compliance

- **Files**: scripts/dev-sync.sh, scripts/dev-sync.ps1, .claude/commands/sync.md, docs/context.md, CLAUDE.md
- **Purpose**: Align project structure with C:\git\CLAUDE.md Project Constitution
- **Decisions**:
  - Added `dev-sync.sh` / `dev-sync.ps1` as Project Constitution §3 standard entry-point wrappers
  - Updated `/sync` command to invoke `dev-sync.sh` instead of `vsp-sync.sh` directly
  - Added `Project Overview`, `Tech Stack`, `Agents`, `Skills` sections to `docs/context.md` (§7 requirement)
  - Added `.claude/commands/` inventory and legacy `commands/` folder note to `CLAUDE.md`
