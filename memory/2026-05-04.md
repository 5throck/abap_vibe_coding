# Memory Log: 2026-05-04

Records ABAP objects created or modified during development sessions, along with key decisions and issue history.

---

## Infrastructure & Configuration

### VSP WebSocket Infrastructure Deployment

- **Date**: 2026-05-04
- **Components**: `ZIF_VSP_SERVICE`, `ZCL_VSP_UTILS`, `ZCL_VSP_RFC_SERVICE`, `ZCL_VSP_DEBUG_SERVICE`, `ZCL_VSP_REPORT_SERVICE`, `ZCL_VSP_APC_HANDLER`
- **Package**: `$TMP`

#### Purpose
Enables stateful WebSocket-based operations including ABAP debugging, dynamic RFC calls, and background report monitoring.

#### Key Technical Decisions (NW 7.52 Compatibility)
- **Regex Patch**: Replaced `FIND PCRE` with `FIND REGEX` as NW 7.52 does not support PCRE.
- **Dynamic Table Typing**: Explicitly typed field symbols as `ANY TABLE` in `ZCL_VSP_RFC_SERVICE` to resolve loop errors on generic data references.
- **Optional Service Decoupling**: Commented out `AMDP` and `Git` service instantiation in `ZCL_VSP_APC_HANDLER` to allow activation in systems where these classes are not yet installed.
- **Pragma Removal**: Removed `##REGEX_POSIX` pragmas which are unsupported in 7.52.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| `ExceptionResourceInvalidLockHandle` | ADT lock conflict during automated deploy | Manually wrote source using `vsp source write` with retry logic. |
| `Field PCRE is unknown` | NW 7.52 compiler limitation | Global replace of PCRE with REGEX in all service classes. |
| `<FS_TAB>` is not an internal table | Generic typing in dynamic LOOP | Added `TYPE ANY TABLE` to field symbol declaration. |
| `wscat` connection 401 | Incorrect Base64 credential encoding | Provided PowerShell script to generate correct Authorization header. |

---

## abapGit Installation

- **Object**: `ZABAPGIT_STANDALONE` (Program)
- **Version**: Standalone 1.12x

#### Key Technical Decisions
- **Rename**: Renamed to `ZABAPGIT_STANDALONE` to avoid collision with standard `ZABAPGIT` developer version.
- **7.52 Patch**: Removed unsupported pragmas to ensure successful activation on NPL system.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| No development license | Trial system (NPL) restriction | Registered developer key via **SOBJ** transaction. |

---

## Documentation & Standardization

- **File**: `CLAUDE.md`
- **Changes**: Removed `### 4. SFLIGHT Demo Application` and `## Current Priorities` sections as per user request to keep the file focused on core setup and rules.
- **File**: `docs/setup-guide.md`
- **Changes**: Updated with detailed installation steps, NW 7.52 compatibility notes, and troubleshooting guide.
- **Localization**: Standardized all documentation to English-only to ensure consistency for global AI agents.

---

## SFLIGHT Query Development

- **Object**: `ZPROG_SFLIGHT_QUERY` (Program)
- **Package**: `$TMP`
- **ADT URL**: `/sap/bc/adt/programs/programs/zprog_sflight_query`

#### Purpose
Provides a professional, ALV-based query interface for the standard SFLIGHT data model, linking carriers, schedules, and individual flights.

#### Key Technical Decisions
- **OO-ALV**: Implemented using `CL_SALV_TABLE` to provide a robust, modern UI with built-in export and filtering.
- **Joins**: Performed inner join between `SCARR`, `SPFLI`, and `SFLIGHT` to provide human-readable carrier names and route descriptions instead of just technical IDs.
- **Dynamic Selection Screen**: Used `SELECT-OPTIONS FOR` to leverage existing Data Dictionary field descriptions as requested by the user.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| `ExceptionResourceInvalidLockHandle` | Known ADT lock bug in environment | Deployed program via `vsp execute` with `INSERT REPORT` as a manual override. |

---

## Cross-Platform Workflow Enhancement

- **Date**: 2026-05-04
- **Components**: `scripts/vsp-task.*`, `scripts/vsp-sync.*`, `contexts/*`
- **Purpose**: Enables seamless development workflow across Windows, MacOS, and Linux environments for all AI tools (Claude, Gemini, Antigravity).

#### Key Technical Decisions
- **Dual Scripting**: Provided both `.ps1` (PowerShell) and `.sh` (Bash) versions for all automation tasks to ensure native support across all platforms.
- **Analyst Context Expansion**: Added BAPI technical specifications and S/4HANA vs Legacy table differences to SD, MM, and FI context files.
- **Task Standardization**: Automated the task handoff process from `docs/task-template.md` to `scratch/` via `vsp-task` scripts.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| `multi_replace_file_content` failure | Incorrect target file selection | Manually restored `task.md` and applied changes to individual context files sequentially. |

---

## Program Restoration

### ZPROG_EPM_DEMO — EPM Sales Order Dashboard (Reinstalled)

- **Date**: 2026-05-04
- **Package**: `$TMP`
- **Object URL**: `/sap/bc/adt/programs/programs/ZPROG_EPM_DEMO`

#### Purpose
Restored the deleted EPM demo program to the system. Provides a modern alternative to SFLIGHT using Enterprise Procurement Model tables.

#### Key Technical Decisions
- **Source Recovery**: Recovered from git history (Pickaxe search) as the local file was previously deleted.
- **Title Restoration**: Manually updated TEXTPOOL ID 'R' via `INSERT TEXTPOOL` to ensure the title `EPM Sales Order Dashboard (ALV)` is correctly displayed.

#### Issue History
| Issue | Root Cause | Resolution |
|-------|-----------|------------|
| `ExceptionResourceInvalidLockHandle` (423) | ADT lock conflict during `vsp deploy` | Used `vsp source write` as a follow-up to successfully update the source code. |

---
*Last Updated: 2026-05-04*
