# 2026-05-05

## README.md Update
- **Object**: `README.md`
- **Purpose**: Enhanced the explanation of the "Harness Engineering" concept and reduced over-emphasis on VSP, renaming the title from "VSP Harness Engineering for ABAP" to "Harness Engineering for SAP ABAP".
- **Key decisions**: Modified the Project Mission and Harness Engineering Concept sections to focus more on the Document-First methodology, role-based AI collaboration, and generalized protocols rather than specific tools like VSP.

## Path Standardization
- **File**: `CLAUDE.md`, `docs/setup-guide.md`, `.mcp.json.sample`
- **Changes**: Replaced all absolute path examples and hardcoded `./vsp.exe` references with standardized relative path `./vsp` to ensure project portability across environments.
- **Verification**: Project-wide scan confirmed no hardcoded absolute paths remain in core configurations.

## Project Configuration & Skills Update
- **Object**: `.mcp.json`, `.gemini/settings.json`, `docs/SKILL.md`, `docs/CONTEXT.md`
- **Purpose**: Reflected evaluation results to ensure cross-platform compatibility and professional ABAP development capabilities.
- **Key decisions**:
    - Changed MCP command from `./vsp.exe` to `./vsp` for platform-agnostic execution.
    - Added specialized SAP skills: `sap:bapi-explorer`, `sap:transport-manager`, `sap:unit-architect`, and `sap:performance-analyzer`.
    - Integrated new skills into the standard "Harness Advanced" workflow in `docs/CONTEXT.md`.

## Directory & Core Documentation Reorganization
- **Action**: Renamed `contexts/` to `SAP ERP Module/` and moved it under `docs/`. Moved `CONTEXT.md`, `MCP_USAGE.md`, `SECURITY.md`, and `SKILL.md` from the root to `docs/`.
- **Purpose**: Consolidate all technical and domain documentation under the `docs/` hierarchy to clean up the root directory and improve project structure.
- **Affected Files**: Updated `README.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `docs/setup-guide.md`, `docs/task-template.md`, and subagent prompts to reflect the new paths.

## ABAP File Isolation Rule
- **Action**: Enforced a new rule that all local `.abap` files must be created exclusively within the `scratch/` directory.
- **Purpose**: Prevent repository clutter and maintain a clean project root while ensuring all AI agents follow a consistent file management policy.
- **Affected Files**: Updated `docs/CONTEXT.md`, `docs/SKILL.md`, `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md`.

## Automated Documentation Audit
- **Action**: Implemented an automated documentation audit script in `scripts/vsp-audit.ps1` and integrated it into the `vsp-sync.ps1` workflow.
- **Features**:
    - **Path Check**: Automatically detects absolute paths (Windows and Unix style) in all `.md` files (excluding `setup-guide.md` and code blocks).
    - **Workflow Integration**: `vsp-sync.ps1` now calls `vsp-audit.ps1` before every commit. If any absolute paths are detected, the commit is blocked.
- **Reliability**: Optimized the implementation to handle platform-specific encoding issues by using a modular script structure.

---

*Last Updated: 2026-05-05*
